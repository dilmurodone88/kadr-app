# ── Bog'liqliklar va build artefaktlari (image ichida qayta quriladi) ──
node_modules
.next
out
build

# ── Git / IDE ──
.git
.gitignore
.github
.vscode
.idea

# ── Docker fayllari (image ichiga kirmaydi) ──
# DIQQAT: docker-entrypoint.sh va scripts/ image ichiga COPY qilinadi — ular ignore QILINMAYDI.
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.*.yml

# ── SIRLAR — HECH QACHON image'ga tushmasin ──
.env
.env.local
.env.*.local
*.pem
*.key

# ── Loglar / vaqtinchalik ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.DS_Store

# ── Hujjatlar (runtime uchun kerak emas) ──
README.md
docs
design-system
*.md
